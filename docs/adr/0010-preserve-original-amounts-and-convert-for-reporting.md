# Preserve original amounts and convert only for reporting

MyFinance preserves every Original Amount and its source currency, then projects values into a selectable Reporting Currency using retained Reference Rate evidence. EUR is the Household default because current Income originates in Belgium, while BRL and USD remain selectable; changing the Reporting Currency changes presentation and aggregation only, never the underlying Consolidated Financial Record.
